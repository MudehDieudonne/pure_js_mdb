{
  "name": "moviedb_js",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8000 --host",
    "build": "vite build && cp ./details.html ./dist/details.html",
    "preview": "vite preview --port 8000"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
